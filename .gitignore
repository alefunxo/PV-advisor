.claude/
CLAUDE.md

# Scratch pages at the repo root, including the data-failure fixture _tests/run.py generates
# from index.html on each run. The suites themselves live in _tests/ and are committed.
_*.html

# Byte-cache from running scripts/fetch_pvgis.py
__pycache__/
scripts/.pvgis-checkpoint.jsonl
